/**
 * Delete comment from commentID
 */

function reportComment(res, commentID, mongodb) {
	// NYI
	console.log("Error : Not yet implemented");
}

module.exports = {
	initialize: function(res, commentID, mongodb) {
		reportComment(res, commentID, mongodb);
	}
};
